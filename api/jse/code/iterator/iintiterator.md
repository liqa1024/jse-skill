# IIntIterator

> `jse.code.iterator.IIntIterator`

**核心职能**：无装箱 int 迭代器，提供 `toIterator()`→JDK Iterator、`forEachRemaining`、`remove` 默认方法，以及 `asDouble()` 跨类型转换。

```java
boolean hasNext()
int next()
```
