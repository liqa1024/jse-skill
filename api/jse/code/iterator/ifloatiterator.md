# IFloatIterator

> `jse.code.iterator.IFloatIterator`

**核心职能**：无装箱 float 迭代器，提供 `toIterator()`→JDK Iterator、`forEachRemaining`、`remove` 默认方法，以及 `asDouble()` 跨类型转换。

```java
boolean hasNext()
float next()
```
