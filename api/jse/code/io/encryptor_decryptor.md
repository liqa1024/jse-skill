# Encryptor / Decryptor

> `jse.code.io.Encryptor` / `jse.code.io.Decryptor`

**核心职能**：使用 SHA-256 派生密钥进行 AES/CBC/PKCS5Padding 加解密，输出 Base64。`new Encryptor(String key)` / `new Decryptor(String key)`。标记 @Deprecated。
