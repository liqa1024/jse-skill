# Iterables

> `jse.code.collection.Iterables`

**核心职能**：`@VisibleForTesting`，`final`，`extends AbstractCollections`。`private` 构造器，静态工具类。所有方法与 `AbstractCollections` 方法对应（参见 `abstractcollections.md`），基于视图模式创建不可变/可变集合。
