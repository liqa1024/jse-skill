# DoubleWrapper

> `jse.code.collection.DoubleWrapper`

**核心职能**：`implements IDoubleWrapper`。单 double 值的可变包装，包含 `mValue` 公共字段。

```java
// === 字段 ===
double mValue

// === 构造器 ===
DoubleWrapper(double)

// === IDoubleWrapper 接口实现 ===
double value()

// === 对象方法 ===
String toString()
```
