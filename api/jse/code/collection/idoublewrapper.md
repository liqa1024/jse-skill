# IDoubleWrapper / DoubleWrapper

> `jse.code.collection.IDoubleWrapper` / `jse.code.collection.DoubleWrapper`

**核心职能**：`IDoubleWrapper` 为单 double 值的只读接口，`DoubleWrapper` 为可变包装（`mValue` 字段）。

```java
// IDoubleWrapper
double getValue()

// DoubleWrapper
double mValue
DoubleWrapper(double)
```
