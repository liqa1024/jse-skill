# IPair / Pair

> `jse.code.collection.IPair` / `jse.code.collection.Pair`

**核心职能**：`IPair<A,B>` 为只读二元组接口（`mA`/`mB` 字段），`Pair<A,B>` 为可变实现。

```java
// IPair<A,B>
A getA() / B getB()

// Pair<A,B>
A mA / B mB
Pair(A, B)
```
