# ArrayLists

> `jse.code.collection.ArrayLists`

**核心职能**：`@VisibleForTesting`，`final`，`extends NewCollections`。`private` 构造器，静态工具类。所有方法与 `NewCollections` 方法对应但每次都创建新 `ArrayList` 或 `T[]`（值拷贝），参见 `newcollections.md`。
