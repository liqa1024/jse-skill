# APC

> `jse.atom.APC`

**核心职能**：`@VisibleForTesting final class extends AtomicParameterCalculator`，类型别名。构造器 `private+@Deprecated`，仅通过 `APC.of()` / `APC.withOf()` 静态工厂使用。所有方法继承自 `AtomicParameterCalculator`。

```java
@VisibleForTesting
final class APC extends AtomicParameterCalculator
```
