# IFunc1Subs

> `jse.math.function.IFunc1Subs`

**核心职能**：一维函数求值 lambda 接口（@FunctionalInterface），作为 `IFunc1.fill(IFunc1Subs)` 的参数，支持 lambda 逐点填充。

```java
@FunctionalInterface
double subs(double aX)                                              // 函数求值 f(x)
```
