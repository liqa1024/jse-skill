# PowerShell

> `jse.system.PowerShell`

**核心职能**：`@VisibleForTesting`，`final`，`extends PowerShellSystemExecutor`。

```java
PowerShell()                       // → PowerShellSystemExecutor(), Base64(UTF-16LE) 编码传参
```
