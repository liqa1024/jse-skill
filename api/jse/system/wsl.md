# WSL

> `jse.system.WSL`

**核心职能**：`@VisibleForTesting`，`final`，`extends WSLSystemExecutor`。

```java
WSL()                              // → WSLSystemExecutor(), programAndArgs_→{"wsl","bash","-c"}
```
