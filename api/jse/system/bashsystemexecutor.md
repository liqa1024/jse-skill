# BashSystemExecutor

> `jse.system.BashSystemExecutor`

**核心职能**：final class extends AbstractSystemExecutor。Bash 命令执行器，通过 `bash -c` 执行命令。

```java
BashSystemExecutor()              // programAndArgs_→{"bash","-c"}
```
