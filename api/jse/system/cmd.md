# CMD

> `jse.system.CMD`

**核心职能**：`@VisibleForTesting`，`final`，`extends CMDSystemExecutor`。

```java
CMD()                              // → CMDSystemExecutor(), programAndArgs_→{"cmd","/c"}, charset_→GBK
```
