# Bash

> `jse.system.Bash`

**核心职能**：`@VisibleForTesting`，`final`，`extends BashSystemExecutor`。

```java
Bash()                             // → BashSystemExecutor(), programAndArgs_→{"bash","-c"}
```
